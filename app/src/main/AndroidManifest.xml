<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Numeralis"
        tools:targetApi="31">

        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.RegisterActivity" android:exported="false" />
        <activity android:name=".activities.logic.SatisfiabilityActivity" android:exported="false" />
        <activity android:name=".activities.logic.TruthTableActivity" android:exported="false" />
        <activity android:name=".activities.logic.BooleanFunctionsActivity" android:exported="false" />
        <activity android:name=".activities.math.ProbabilityActivity" android:exported="false" />
        <activity android:name=".activities.math.CalculusActivity" android:exported="false" />
        <activity android:name=".activities.math.BaseConversionActivity" android:exported="false" />
        <activity android:name=".activities.math.ArithmeticActivity" android:exported="false" />

    </application>

</manifest>
